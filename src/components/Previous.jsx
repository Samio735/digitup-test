import { Button } from "@mui/material";

export default function Previous() {
  return (
    <Button
      variant="contained"
      sx={{ boxShadow: "none", textTransform: "none" }}
    >
      Previous
    </Button>
  );
}
